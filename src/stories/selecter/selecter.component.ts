import { Component, Input, Output, EventEmitter } from '@angular/core';

@Component({
  selector: 'selecter',
  templateUrl: './selecter.component.html',
  styleUrls: ['./selecter.css'],
})
export default class SelecterComponent {
  
}
